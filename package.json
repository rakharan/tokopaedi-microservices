{
  "name": "tokopaedi-microservices",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "docker-compose up",
    "dev:services": "docker-compose up user-service product-service order-service",
    "dev:infra": "docker-compose up rabbitmq redis user-db product-db order-db",
    "stop": "docker-compose down",
    "clean": "docker-compose down -v",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}